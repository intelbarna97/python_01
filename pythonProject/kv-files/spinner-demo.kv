<MainWindow>:
    Spinner:
        text:"Home"
        size_hint:0.2,0.3
        pos_hint:{"center":(0.5,0.8)}
        #values:"Home", "Work", "Other", "Custom"
        value:root.values_list
        on_text:print("The spinner {} has text {}".format(self,self.text))